{
  "appSettings": {
    "cacheConfiguration": {
      "connectionString": "surchargé par var env ou profile",
      "timeToLive": "surchargé par var env ou profile",
      "instanceName": "panier_" // anti cache poisoning
    },
    "authorityConfiguration": {
      "authority": "surchargé par var env ou profile",
      "audience": "surchargé par var env ou profile"
    },
    "catalogueServiceConfiguration": {
      "url": "overriden_by_environment_variable"
    },
    "routePrefix": "surchargé par var env",
    "enableSwagger": "surchargé par var env",
    "httpRequestScheme": "surchargé par var env",
    "deployedVersion": "surchargé par var env"
  }
}
