{
  "appSettings": {
    "authorityConfiguration": {
      "authority": "https://fake.url",
      "audience": "https://fake.url"
    },
    "routePrefix": "surchargé par var env",
    "enableSwagger": "false",
    "httpRequestScheme": "surchargé par var env",
    "deployedVersion": "surchargé par var env",
    "kafkaConfiguration": {
      "brokers": "surchargé par var env",
      "consumerGroup": "surchargé par var env"
    }
  }
}
